#!/bin/bash
# [copy-paste] bash <(curl -s https://raw.githubusercontent.com/sophalHong/.my-terminal/master/.one-click)

command -v git &> /dev/null || \
	sudo apt install -y git || \
	sudo yum install -y git

git clone https://github.com/sophalHong/.my-terminal.git && bash .my-terminal/install.sh all
